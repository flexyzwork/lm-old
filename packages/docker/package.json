{
  "name": "@packages/docker",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "exports": {},
  "publishConfig": {},
  "scripts": {
    "dev": "docker-compose up",
    "reset": "docker-compose rm --force --stop dynamodb-local && docker-compose up -d",
    "seed": "tsx ../../apps/server/src/seed/seedDynamodb.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.3"
  }
}
